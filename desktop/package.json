{
  "name": "cc-gui-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop GUI wrapper for Claude Code CLI",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:python": "pyinstaller build/pyinstaller.spec --distpath src-tauri/binaries/server --workpath build/pyinstaller-work --clean --noconfirm",
    "build:all": "npm run build:python && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
